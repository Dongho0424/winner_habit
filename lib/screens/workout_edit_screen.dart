import 'package:flutter/material.dart';

class WorkOutEditScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("WorkOutEditScreen"),centerTitle: true,),
      body: Container(
        child: Text("WorkOutEditScreen!"),
      ),
    );
  }
}
